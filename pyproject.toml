[project]
name = "react-agent"
version = "0.1.0"
description = "LangGraph ReAct agent"
authors = [
    {name = "Viktor Szabo",email = "szabo_viktor@lilly.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
