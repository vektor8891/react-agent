# Google Cloud Configuration
# Copy this file to .env and fill in your values

# Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# Location/Region for Vertex AI (required for Vertex AI)
GOOGLE_CLOUD_LOCATION=us-central1

# Staging bucket for Vertex AI (optional - will default to PROJECT_ID-staging)
GOOGLE_CLOUD_STAGING_BUCKET=gs://your-staging-bucket-name

# For local development: use Application Default Credentials
# Run: gcloud auth application-default login

# For production deployment: specify service account key file
GOOGLE_APPLICATION_CREDENTIALS=/Users/L015680/Git/react-agent/credentials/service-account-key.json

# For Google Cloud services (Cloud Run, GKE): use Workload Identity
# No credentials file needed - uses service identity

# Optional: Default bucket for storage operations
# GCS_BUCKET_NAME=your-default-bucket

# Environment type (helps determine auth method)
# ENVIRONMENT=local|production|cloud
